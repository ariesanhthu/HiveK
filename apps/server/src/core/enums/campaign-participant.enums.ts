export enum EParticipantStatus {
  PENDING_APPROVAL = 'pending_approval',
  JOINED = 'joined',
  COMPLETED = 'completed',
  REJECTED = 'rejected',
}

export enum EOutputStatus {
  DRAFT = 'draft',
  SCHEDULED = 'scheduled',
  PUBLISHED = 'published',
  FAILED = 'failed',
}

export enum EOutputType {
  VIDEO = 'video',
  SHORT_VIDEO = 'short_video',
  POST = 'post',
  BLOG = 'blog',
}
