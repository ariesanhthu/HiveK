import * as React from 'react';

/**
 * PublishingTimeline — from client@0.1.0.
 */
export interface PublishingTimelineProps {
  [key: string]: unknown;
}

export declare const PublishingTimeline: React.ComponentType<PublishingTimelineProps>;
